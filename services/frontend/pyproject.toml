[project]
name = "prscribe-frontend"
version = "0.1.0"
description = "Streamlit demo UI for PRScribe."
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.36.0",
    "prscribe-orchestration",
    "python-dotenv>=1.0.0",
]

[tool.uv.sources]
prscribe-orchestration = { workspace = true }
